**1. Which HTML element represents the main content unique to a page?**
a) `<header>`
b) `<aside>`
*c) `<main>`*
d) `<footer>`

💡 **Background:** HTML5 introduced semantic elements to clarify structure.
🧠 **Feedback:** `<main>` identifies the central, unique content on a page—screen readers use it to skip directly to meaningful information.

---

**2. Why is semantic HTML important?**
a) It makes code shorter
*b) It improves accessibility and SEO by giving elements meaning*
c) It loads faster on mobile
d) It changes font colors automatically

💡 **Background:** Semantics describe *what* content is, not *how* it looks.
🧠 **Feedback:** Semantic tags help assistive technologies interpret structure, and search engines rank content more accurately.

---

**3. Which CSS property is used to control the space *outside* an element’s border?**
*a) margin*
b) padding
c) border-width
d) gap

💡 **Background:** The CSS box model defines how spacing is calculated.
🧠 **Feedback:** `margin` pushes elements apart; `padding` adds internal spacing inside the border.

---

**4. What does responsive design ensure?**
*a) The website adapts to different devices and screen sizes*
b) The website uses animations
c) The website hides navigation on mobile
d) The website scrolls automatically

💡 **Background:** Responsive design uses flexible grids and media queries.
🧠 **Feedback:** A responsive layout adjusts for phone, tablet, and desktop screens—improving usability across all viewports.

---

**5. Which HTML tag defines a self-contained piece of content that could stand alone?**
a) `<div>`
*b) `<article>`*
c) `<span>`
d) `<section>`

💡 **Background:** `<article>` is meant for independent items like news posts or blog entries.
🧠 **Feedback:** An `<article>` can be reused or syndicated without losing context—unlike generic `<div>` containers.

---

**6. What is the correct syntax for linking an external CSS file?**
*a) `<link rel="stylesheet" href="style.css">`*
b) `<style src="style.css">`
c) `<css link="style.css">`
d) `<stylesheet href="style.css">`

💡 **Background:** The `<link>` tag in the `<head>` connects the HTML to its stylesheet.
🧠 **Feedback:** Using `rel="stylesheet"` tells the browser to apply the CSS rules from `style.css`.

---

**7. The `<alt>` attribute in an `<img>` tag is used to:**
a) Add captions under images
*b) Provide text alternatives for screen readers*
c) Increase image resolution
d) Create tooltips

💡 **Background:** Alt text is an accessibility requirement.
🧠 **Feedback:** The `alt` attribute describes images for users who can’t see them and for cases where images fail to load.

---

**8. In Flexbox, which property defines how items align along the main axis?**
a) align-items
b) align-content
*c) justify-content*
d) flex-grow

💡 **Background:** The main axis is the primary direction Flexbox lays out children.
🧠 **Feedback:** `justify-content` controls spacing along that axis—useful for “logo left, nav right” headers.

---

**9. What is the correct HTML5 declaration at the start of a web page?**
a) `<doctype html5>`
*b) `<!DOCTYPE html>`*
c) `<html version="5">`
d) `<doctype="html">`

💡 **Background:** The doctype triggers standards mode in browsers.
🧠 **Feedback:** `<!DOCTYPE html>` tells browsers to interpret code using modern HTML5 rules.

---

**10. Which CSS unit scales relative to the root font size?**
a) %
b) px
*c) rem*
d) vh

💡 **Background:** `rem` = “root em” (based on the `<html>` font size).
🧠 **Feedback:** Using `rem` ensures consistent scaling across the site, improving accessibility and design consistency.

---

**11. How can developers test if HTML is correctly structured?**
*a) Use the W3C Validator tool*
b) Use Photoshop layers
c) Check it in Chrome only
d) Copy code into Word

💡 **Background:** Validation ensures compliance with HTML standards.
🧠 **Feedback:** The W3C Markup Validator flags syntax errors, missing tags, and structural issues for correction.

---

**12. Why should developers use external stylesheets instead of inline CSS?**
*a) To separate structure and presentation for easier maintenance*
b) To make files longer
c) To reduce color usage
d) To limit responsiveness

💡 **Background:** Separation of concerns improves maintainability.
🧠 **Feedback:** External CSS keeps code cleaner and allows global changes without editing each HTML file.

---

**13. What does `display: flex;` do in CSS?**
*a) Creates a flexible container for layout alignment*
b) Adds animation effects
c) Centers text automatically
d) Changes background color

💡 **Background:** Flexbox simplifies layout alignment across devices.
🧠 **Feedback:** Using `display:flex` lets you control direction, spacing, and alignment—key for modern, responsive headers and footers.

---

**14. The `<footer>` tag should contain:**
*a) Copyright and contact information*
b) Page headings
c) Navigation links only
d) Image galleries

💡 **Background:** The footer defines closing content for a section or page.
🧠 **Feedback:** Place legal text, credits, and links to privacy policies in the `<footer>` for clarity and accessibility.

---

**15. Why is a consistent folder structure important in web projects?**
a) It improves image quality
*b) It keeps assets organized for collaboration and deployment*
c) It reduces bandwidth
d) It hides unused files

💡 **Background:** Web projects often involve teams and version control.
🧠 **Feedback:** Consistent folders (`/css`, `/images`, `/scripts`) prevent broken links and simplify uploads to GitHub Pages.

---

**16. Which property creates rounded corners in CSS?**
a) border-style
*b) border-radius*
c) border-spacing
d) corner-round

💡 **Background:** Visual presentation in CSS controls element edges.
🧠 **Feedback:** `border-radius` produces curved edges for buttons, images, and cards without extra graphics.

---

**17. Which of the following is true about responsive images?**
a) They load slower on desktop
*b) They adapt to the device’s display size*
c) They use GIF only
d) They ignore aspect ratios

💡 **Background:** HTML5 introduced `srcset` and `picture` elements.
🧠 **Feedback:** *Responsive images* serve the best-fit version for each device, improving speed and accessibility.

---

**18. How can you check your site’s accessibility performance?**
*a) Use Lighthouse or WAVE tools*
b) Check file sizes
c) Review JavaScript logs
d) Use Notepad

💡 **Background:** Accessibility tools measure contrast, ARIA roles, and navigation issues.
🧠 **Feedback:** Tools like WAVE and Lighthouse highlight areas needing improvement for WCAG compliance.

---

**19. In a responsive grid, the `fr` unit represents:**
a) Font ratio
*b) Fraction of available space*
c) Frame rate
d) Flow rate

💡 **Background:** CSS Grid uses fractional units to allocate space dynamically.
🧠 **Feedback:** Using `fr` units ensures columns resize proportionally to the container.

---

**20. What is the correct way to comment in CSS?**
a) `<!-- comment -->`
*b) `/* comment */`*
c) `// comment //`
d) `[comment]`

💡 **Background:** CSS comments use the slash-asterisk syntax.
🧠 **Feedback:** Proper comments help document styles for team members and future edits.

---

**21. Which of the following supports mobile-first CSS?**
*a) Using min-width media queries*
b) Using fixed-width layouts
c) Using only px units
d) Centering all elements

💡 **Background:** Mobile-first design styles for small screens first, then enhances for larger ones.
🧠 **Feedback:** `@media (min-width: ...)` adds rules progressively, ensuring graceful scaling across devices.

---

**22. What is the function of the `<nav>` element?**
a) Displays all links in the footer
*b) Groups navigation links in a semantic block*
c) Embeds external scripts
d) Adds anchor icons

💡 **Background:** The `<nav>` element defines major navigation sections.
🧠 **Feedback:** Using `<nav>` improves accessibility because assistive technologies recognize it as a navigation region.

---

**23. What happens if you omit the `lang` attribute in the `<html>` tag?**
a) The page loads faster
*b) Screen readers may misinterpret pronunciation*
c) CSS stops loading
d) The title disappears

💡 **Background:** The `lang` attribute tells assistive tech which language to use.
🧠 **Feedback:** Without it, screen readers guess the language, causing pronunciation errors for multilingual sites.

---

**24. Why should developers test across multiple browsers?**
*a) To ensure consistent rendering and behavior*
b) To make pages load faster
c) To increase HTML size
d) To test server speeds

💡 **Background:** Browsers interpret CSS and HTML slightly differently.
🧠 **Feedback:** Cross-browser testing reveals inconsistencies early and ensures a uniform user experience.

---

**25. Why is GitHub Pages used for WEBD1000 projects?**
a) To edit code directly online
*b) To host static HTML/CSS sites for free portfolio display*
c) To create video tutorials
d) To design wireframes

💡 **Background:** GitHub Pages serves static files from a repository at no cost.
🧠 **Feedback:** *Publishing to GitHub Pages* lets students showcase validated HTML/CSS projects as live websites.
